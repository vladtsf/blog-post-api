{
  "name": "blog-post-api",
  "version": "0.0.1",
  "scripts": {
    "start": "node app",
    "test": "./node_modules/mocha/bin/mocha --ui bdd --timeout 2000 --compilers coffee:coffee-script --colors --reporter spec test/functional/*.coffee --require should --require test/helpers.coffee",
    "deploy": "git push heroku `git rev-parse --abbrev-ref HEAD`:master"
  },
  "dependencies": {
    "coffee-script": "*",
    "underscore": "*",
    "backbone": "*",
    "backbone-validation": "*",
    "express-namespace": "*",
    "mongoose": "3.1.2",
    "express": "3.0.1",
    "supertest": "*",
    "mocha": "*",
    "should": "*"
  },
  "devDependencies": {
  },
  "engines": {
    "node": "0.8.x",
    "npm": "1.1.x"
  }
}