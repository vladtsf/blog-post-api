{
  "name": "blog-post-api",
  "version": "0.0.1",
  "scripts": {
    "start": "node app",
    "test": "./node_modules/mocha/bin/mocha --ui bdd --timeout 5000 --slow 500 --compilers coffee:coffee-script --colors --reporter spec test/functional/*.coffee --require should --require test/helpers.coffee"
  },
  "dependencies": {
    "coffee-script": "1.4.0",
    "express-namespace": "0.1.1",
    "mongoose": "3.1.2",
    "express": "3.0.1",
    "async": "0.1.22",
    "supertest": "0.4.2",
    "mocha": "1.7.0",
    "should": "1.2.1"
  },
  "devDependencies": {
  },
  "engines": {
    "node": "0.8.x",
    "npm": "1.1.x"
  }
}